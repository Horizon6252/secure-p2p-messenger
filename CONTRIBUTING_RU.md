# Внесение вклада в Secure P2P Messenger

Спасибо за ваш интерес к внесению вклада в Secure P2P Messenger! 🎉

[English](CONTRIBUTING.md) | [Русский](#русский)

---

## Русский

## Содержание

1. [Кодекс поведения](#кодекс-поведения)
2. [Как я могу внести вклад?](#как-я-могу-внести-вклад)
3. [Настройка разработки](#настройка-разработки)
4. [Стандарты кодирования](#стандарты-кодирования)
5. [Руководство по коммитам](#руководство-по-коммитам)
6. [Процесс Pull Request](#процесс-pull-request)
7. [Проблемы безопасности](#проблемы-безопасности)

---

## Кодекс поведения

### Наше обещание

Мы стремимся обеспечить гостеприимную и инклюзивную среду для всех участников, независимо от:
- Уровня опыта
- Гендерной идентичности
- Сексуальной ориентации
- Инвалидности
- Внешности
- Расы или этнической принадлежности
- Возраста
- Религии

### Ожидаемое поведение

✅ **Делайте:**
- Будьте уважительны и внимательны
- Приветствуйте новичков и помогайте им учиться
- Принимайте конструктивную критику с достоинством
- Фокусируйтесь на том, что лучше для проекта
- Проявляйте эмпатию к другим

❌ **Не делайте:**
- Не используйте оскорбительный, дискриминационный или домогающийся язык
- Не совершайте личных атак
- Не публикуйте частную информацию других людей
- Не занимайтесь троллингом или провокационным поведением

---

## Как я могу внести вклад?

### 🐛 Сообщение об ошибках

Перед созданием отчёта об ошибке, пожалуйста, проверьте существующие issues, чтобы избежать дубликатов.

**Хороший отчёт об ошибке включает:**
- Чёткий, описательный заголовок
- Шаги воспроизведения
- Ожидаемое vs фактическое поведение
- Скриншоты (если применимо)
- Детали окружения:
  - ОС (версия Android/Windows)
  - Версия приложения
  - Версия сервера

**Шаблон:**
```markdown
**Описание:**
Краткое описание ошибки

**Шаги воспроизведения:**
1. Перейдите к '...'
2. Нажмите на '...'
3. Увидите ошибку

**Ожидаемое поведение:**
Что должно произойти

**Фактическое поведение:**
Что происходит на самом деле

**Окружение:**
- ОС: Windows 11 / Android 13
- Версия приложения: 2.0.0
- Версия сервера: 6.0

**Скриншоты:**
Если применимо
```

### 💡 Предложение функций

Запросы функций приветствуются! Пожалуйста:

1. Проверьте, не запрошена ли функция уже
2. Объясните случай использования и преимущества
3. Подумайте, подходит ли это в область проекта
4. Будьте открыты к обсуждению

**Шаблон:**
```markdown
**Описание функции:**
Какую функцию вы хотите?

**Случай использования:**
Зачем это нужно?

**Предлагаемое решение:**
Как бы вы это реализовали?

**Рассмотренные альтернативы:**
Другие варианты, которые вы обдумали

**Дополнительный контекст:**
Скриншоты, макеты и т.д.
```

### 📝 Улучшение документации

Улучшения документации всегда приветствуются:
- Исправление опечаток или неясных объяснений
- Добавление недостающей информации
- Улучшение примеров
- Перевод на другие языки

### 💻 Вклад в код

Мы принимаем вклады для:
- Исправления ошибок
- Новых функций
- Улучшения производительности
- Рефакторинга кода
- Покрытия тестами

---

## Настройка разработки

### Требования

**Обязательно:**
- Flutter 3.0+
- Dart SDK 3.0+
- Git

**Для Android:**
- Android Studio
- Android SDK (API 21+)

**Для Windows:**
- Visual Studio 2019+ с разработкой C++ для десктопа

### Инструкции по настройке

1. **Форкните репозиторий:**
```bash
# Нажмите кнопку "Fork" на GitHub
```

2. **Клонируйте свой форк:**
```bash
git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/secure-p2p-messenger.git
cd secure-p2p-messenger
```

3. **Добавьте upstream remote:**
```bash
git remote add upstream https://github.com/sssilverhand/secure-p2p-messenger.git
```

4. **Установите зависимости:**
```bash
flutter pub get
```

5. **Настройте для разработки:**
```bash
# Скопируйте пример конфига
cp lib/config.example.dart lib/config.dart

# Отредактируйте config.dart с вашим тестовым сервером
nano lib/config.dart
```

6. **Запустите приложение:**
```bash
# Для Android
flutter run

# Для Windows
flutter run -d windows
```

---

## Стандарты кодирования

### Стиль Dart/Flutter

Следуйте руководству [Effective Dart](https://dart.dev/guides/language/effective-dart):

**✅ Хорошо:**
```dart
// Понятные имена переменных
final String userName = 'Alice';
final List<Message> messages = [];

// Правильное форматирование
Future<void> sendMessage(String content) async {
  final encrypted = await CryptoEngine.encrypt(content, key);
  RelayService.sendMessage(contactId, {'type': 'text', 'body': encrypted});
}

// Комментарии для сложной логики
// Генерация общего секрета с использованием X25519 ECDH
final sharedSecret = await algorithm.sharedSecretKey(
  keyPair: myPrivateKey,
  remotePublicKey: theirPublicKey,
);
```

**❌ Плохо:**
```dart
// Неясные имена
final a = 'Alice';
final m = [];

// Плохое форматирование
Future<void>sendMessage(String c)async{final e=await CryptoEngine.encrypt(c,k);RelayService.sendMessage(cId,{'type':'text','body':e});}

// Нет комментариев для сложного кода
final s = await alg.sharedSecretKey(keyPair: k1, remotePublicKey: k2);
```

### Организация кода

```
lib/
├── main.dart                 # Точка входа
├── config.dart              # Конфигурация
├── models/                  # Модели данных
│   ├── contact.dart
│   └── message.dart
├── services/               # Бизнес-логика
│   ├── crypto_engine.dart
│   ├── relay_service.dart
│   └── storage_service.dart
├── screens/               # UI экраны
│   ├── identity_screen.dart
│   ├── contacts_screen.dart
│   └── chat_screen.dart
└── widgets/              # Переиспользуемые компоненты
    └── message_bubble.dart
```

### Соглашения об именовании

- **Классы:** `PascalCase` (например, `CryptoEngine`, `RelayService`)
- **Функции:** `camelCase` (например, `sendMessage`, `generateKey`)
- **Константы:** `SCREAMING_SNAKE_CASE` (например, `MAX_FILE_SIZE`)
- **Приватные:** Префикс `_` (например, `_privateMethod`)

### Комментарии

```dart
// Хорошо: Объясняйте ПОЧЕМУ, а не ЧТО
// Используем X25519 вместо Ed25519, потому что Ed25519 для подписи, не для обмена ключами
final algorithm = X25519();

// Плохо: Избыточно
// Создаём алгоритм
final algorithm = X25519();
```

### Обработка ошибок

```dart
// Всегда обрабатывайте ошибки изящно
try {
  await riskyOperation();
} catch (e) {
  print('Операция не удалась: $e');
  // Показать дружелюбное сообщение пользователю
  ScaffoldMessenger.of(context).showSnackBar(
    SnackBar(content: Text('Не удалось отправить сообщение')),
  );
}
```

---

## Руководство по коммитам

### Формат сообщения коммита

```
<тип>(<область>): <тема>

<тело>

<подвал>
```

### Типы

- **feat:** Новая функция
- **fix:** Исправление ошибки
- **docs:** Изменения в документации
- **style:** Форматирование кода (без изменения логики)
- **refactor:** Реструктуризация кода
- **test:** Добавление тестов
- **chore:** Задачи обслуживания

### Примеры

**Хорошие коммиты:**
```
feat(crypto): реализация обмена ключами X25519

Заменил Ed25519 на X25519 для обмена ключами, так как Ed25519
предназначен для цифровых подписей, а не для ECDH.

Fixes #42
```

```
fix(chat): предотвращение дублирования сообщений при переподключении

Сообщения дублировались при переподключении пользователя
из-за отсутствия логики дедупликации в обработчике сообщений.

Closes #56
```

```
docs(readme): добавлены инструкции по установке для macOS

Добавлен раздел с установкой через Homebrew и
настройкой Flutter на системах macOS.
```

**Плохие коммиты:**
```
исправил всё
```

```
WIP
```

```
Обновил файлы
```

---

## Процесс Pull Request

### Перед отправкой

1. **Обновите свой форк:**
```bash
git fetch upstream
git checkout main
git merge upstream/main
```

2. **Создайте ветку функции:**
```bash
git checkout -b feature/amazing-feature
```

3. **Внесите изменения:**
```bash
# Редактируйте файлы
# Тщательно тестируйте
```

4. **Протестируйте изменения:**
```bash
# Запустите на Android
flutter run

# Запустите на Windows
flutter run -d windows

# Проверьте на проблемы
flutter analyze
```

5. **Закоммитьте с хорошим сообщением:**
```bash
git add .
git commit -m "feat(feature): добавлена потрясающая функция"
```

6. **Запушьте в свой форк:**
```bash
git push origin feature/amazing-feature
```

### Создание Pull Request

1. Перейдите в свой форк на GitHub
2. Нажмите "New Pull Request"
3. Выберите base: `main` ← compare: `feature/amazing-feature`
4. Заполните шаблон PR:

```markdown
## Описание
Что делает этот PR?

## Тип изменения
- [ ] Исправление ошибки
- [ ] Новая функция
- [ ] Обновление документации
- [ ] Улучшение производительности

## Тестирование
Как это было протестировано?
- [ ] Android
- [ ] Windows
- [ ] Ручное тестирование
- [ ] Автоматизированные тесты

## Чеклист
- [ ] Код следует стилю проекта
- [ ] Комментарии добавлены для сложной логики
- [ ] Документация обновлена
- [ ] Нет новых предупреждений
- [ ] Протестировано на целевой платформе(ах)

## Скриншоты (если применимо)
Добавьте скриншоты изменений UI
```

### Процесс ревью

1. **Автоматические проверки:** CI запустится (если настроено)
2. **Ревью кода:** Мейнтейнер проверит ваш код
3. **Запрошенные изменения:** Устраните замечания
4. **Одобрение:** После одобрения PR будет смержен

### После мержа

1. **Удалите ветку:**
```bash
git branch -d feature/amazing-feature
git push origin --delete feature/amazing-feature
```

2. **Обновите свой форк:**
```bash
git checkout main
git pull upstream main
```

---

## Проблемы безопасности

**⚠️ НЕ открывайте публичные issues для уязвимостей безопасности!**

Вместо этого:
1. Email: sssilvercock@gmail.com
2. Включите:
   - Описание уязвимости
   - Шаги воспроизведения
   - Потенциальное влияние
3. Мы ответим в течение 24 часов
4. Координированное раскрытие после выпуска патча

См. [SECURITY_RU.md](SECURITY_RU.md) для полной политики.

---

## Советы по разработке

### Отладка

```dart
// Используйте print стратегически
print('🔐 Шифрование сообщения: ${message.substring(0, 20)}...');
print('✅ Сообщение отправлено: ${contactId}');
print('❌ Ошибка: $e');
```

### Тестирование на нескольких устройствах

1. Запустите два экземпляра (разные коды доступа)
2. Тест доставки сообщений
3. Тест очереди офлайн сообщений
4. Тест звонков (требуется настройка Agora)

### Производительность

- Профилируйте с Flutter DevTools
- Проверяйте утечки памяти
- Мониторьте использование сети
- Тестируйте на слабых устройствах

---

## Получение помощи

- **Вопросы:** [GitHub Discussions](https://github.com/sssilverhand/secure-p2p-messenger/discussions)
- **Ошибки:** [GitHub Issues](https://github.com/sssilverhand/secure-p2p-messenger/issues)
- **Чат:** Присоединяйтесь к нашему сообществу (если доступно)

---

## Признание

Участники будут:
- Добавлены в [CONTRIBUTORS.md](CONTRIBUTORS.md)
- Упомянуты в примечаниях к релизу
- Признаны в документации проекта

---

## Лицензия

Внося вклад, вы соглашаетесь, что ваши вклады будут лицензированы под [GPL-3.0 License](LICENSE).

---

**Спасибо за вклад! 🙏**

Каждый вклад, каким бы маленьким он ни был, помогает сделать этот проект лучше для всех.

**Сделано с ❤️ автором [sssilverhand](https://github.com/sssilverhand)**
